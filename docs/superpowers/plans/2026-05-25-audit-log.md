# Audit Log §5 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Audit log pentru modificările de cost (campaign budget + participant fee) și pentru schimbările de ponderi scoring, cu UI pe `/campaigns/[id]` (tab nou) și `/admin/scoring-settings` (section nou).

**Architecture:** Două tabele append-only (`campaign_cost_history` + `scoring_settings_history`) pe pattern-ul `influencer_rate_card_history` din Sprint 14b. Două helpers (`lib/campaigns/audit.ts`, `lib/scoring/audit.ts`) cu logic best-effort (eșec audit NU blochează update-ul). Trei PATCH endpoints wraparound + un GET endpoint nou pentru cost history. UI: extend `CampaignTabsShell` cu tab "Istoric" + section nouă pe admin.

**Tech Stack:** Supabase Postgres + RLS, TypeScript strict, Next.js 16 App Router server components + client tabs.

**Spec:** `docs/superpowers/specs/2026-05-25-audit-log-design.md`

---

## File Map

| Fișier | Acțiune | Responsabilitate |
|--------|---------|------------------|
| `supabase/migrations/043_campaign_cost_history.sql` | Create | Tabel + enum + CHECK + index + RLS |
| `supabase/migrations/044_scoring_settings_history.sql` | Create | Tabel + index + RLS |
| `lib/campaigns/audit.ts` | Create | `logCostChange()` helper |
| `lib/scoring/audit.ts` | Create | `diffWeights()` + `logWeightsChange()` |
| `app/api/campaigns/[id]/route.ts` | Modify | Wrap PATCH cu audit pe `total_budget` |
| `app/api/campaigns/[id]/participants/[participant_id]/route.ts` | Modify | Wrap PATCH cu audit pe `agreed_fee` |
| `app/api/admin/scoring-settings/route.ts` | Modify | Wrap PATCH cu audit weights |
| `app/api/campaigns/[id]/cost-history/route.ts` | Create | GET endpoint cu Path A scoping |
| `app/campaigns/[id]/campaign-audit-tab.tsx` | Create | Client component cu fetch + tabel |
| `app/campaigns/[id]/tabs-shell.tsx` | Modify | Add tab "Istoric" |
| `app/campaigns/[id]/page.tsx` | Modify | Pasează `audit` slot la tabs-shell |
| `app/admin/scoring-settings/page.tsx` | Modify | Fetch history + pasează la section |
| `app/admin/scoring-settings/weights-history-section.tsx` | Create | Section cu lista 10 changes |

---

## Task 1: Migrations 043 + 044

**Files:**
- Create: `supabase/migrations/043_campaign_cost_history.sql`
- Create: `supabase/migrations/044_scoring_settings_history.sql`

**Context:** Pattern identic cu `influencer_rate_card_history` (migration 040). Cost history are CHECK constraint care enforce coerența între `cost_type` și `participant_id`. Weights history e snapshot complet + diff.

- [ ] **Step 1: Creează `supabase/migrations/043_campaign_cost_history.sql`**

Fișier nou cu conținut:

```sql
-- Sprint 15 Faza 4 §5: audit pentru modificările de cost (campaign budget + participant fee).
--
-- Pattern oglindit după influencer_rate_card_history (Sprint 14b): append-only,
-- one row per real change, idempotent (insert doar la diff real).
--
-- Single table cu enum cost_change_type acoperă ambele cazuri (budget la nivel
-- campanie + fee la nivel participant) cu CHECK constraint care enforce consistency:
--   total_budget → participant_id IS NULL
--   agreed_fee   → participant_id IS NOT NULL

DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'cost_change_type') THEN
    CREATE TYPE cost_change_type AS ENUM ('total_budget', 'agreed_fee');
  END IF;
END $$;

CREATE TABLE IF NOT EXISTS campaign_cost_history (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  campaign_id uuid NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE,
  participant_id uuid REFERENCES campaign_participants(id) ON DELETE CASCADE,
  cost_type cost_change_type NOT NULL,
  amount_before numeric,
  amount_after numeric,
  changed_by uuid REFERENCES team_members(id) ON DELETE SET NULL,
  changed_at timestamptz NOT NULL DEFAULT now(),
  CHECK (
    (cost_type = 'total_budget' AND participant_id IS NULL)
    OR (cost_type = 'agreed_fee' AND participant_id IS NOT NULL)
  )
);

CREATE INDEX IF NOT EXISTS idx_cost_history_campaign
  ON campaign_cost_history(campaign_id, changed_at DESC);

ALTER TABLE campaign_cost_history ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS cost_history_read_authn ON campaign_cost_history;
CREATE POLICY cost_history_read_authn ON campaign_cost_history
  FOR SELECT USING (auth.role() = 'authenticated');

COMMENT ON TABLE campaign_cost_history IS
  'Audit append-only pentru modificarile de cost. Un rand per (campaign, cost_type, [participant]) per modificare reala.';
```

- [ ] **Step 2: Apply migration 043 via Supabase MCP**

Folosește `mcp__claude_ai_Supabase__apply_migration`:
- `project_id`: `uhriwdjhzyorogvukcnv`
- `name`: `043_campaign_cost_history`
- `query`: conținutul SQL fără comentariile leading (păstrează doar SQL executabil)

Expected: `{"success": true}`.

- [ ] **Step 3: Verify migration 043**

```sql
SELECT typname FROM pg_type WHERE typname = 'cost_change_type';
SELECT column_name, data_type FROM information_schema.columns
  WHERE table_name = 'campaign_cost_history' ORDER BY ordinal_position;
```

Expected: enum `cost_change_type` exists; coloane: id, campaign_id, participant_id, cost_type, amount_before, amount_after, changed_by, changed_at.

- [ ] **Step 4: Creează `supabase/migrations/044_scoring_settings_history.sql`**

Fișier nou:

```sql
-- Sprint 15 Faza 4 §5: audit pentru schimbarile ponderilor scoring.
--
-- Extinde scoring_settings.updated_by/at (timestamp only) cu snapshot complet
-- before/after + diff calculat in API. Pattern identic cu influencer_rate_card_history.
--
-- changes jsonb shape (computed in API):
--   { "weight_engagement_rate": { "old": 25, "new": 30 },
--     "weight_cpv": { "old": 20, "new": 15 } }

CREATE TABLE IF NOT EXISTS scoring_settings_history (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  weights_before jsonb,
  weights_after jsonb,
  changes jsonb,
  changed_by uuid REFERENCES team_members(id) ON DELETE SET NULL,
  changed_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_scoring_settings_history_time
  ON scoring_settings_history(changed_at DESC);

ALTER TABLE scoring_settings_history ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS scoring_settings_history_read_authn ON scoring_settings_history;
CREATE POLICY scoring_settings_history_read_authn ON scoring_settings_history
  FOR SELECT USING (auth.role() = 'authenticated');

COMMENT ON TABLE scoring_settings_history IS
  'Audit append-only pentru schimbarile ponderilor scoring. Insert doar la diff real.';
```

- [ ] **Step 5: Apply migration 044 via MCP**

Folosește `mcp__claude_ai_Supabase__apply_migration`:
- `name`: `044_scoring_settings_history`
- `query`: conținutul SQL

Expected: `{"success": true}`.

- [ ] **Step 6: Verify migration 044**

```sql
SELECT column_name, data_type FROM information_schema.columns
  WHERE table_name = 'scoring_settings_history' ORDER BY ordinal_position;
```

Expected: id, weights_before, weights_after, changes, changed_by, changed_at.

- [ ] **Step 7: Commit migrations**

```bash
git add supabase/migrations/043_campaign_cost_history.sql supabase/migrations/044_scoring_settings_history.sql
git commit -m "feat(db): migrations 043+044 — audit log cost + ponderi scoring"
```

---

## Task 2: Helpers audit

**Files:**
- Create: `lib/campaigns/audit.ts`
- Create: `lib/scoring/audit.ts`

**Context:** Helpers care fac insert best-effort (try/catch + console.error pe failure). Idempotency: skip insert dacă `before === after` (cost) sau `changes` empty (weights).

- [ ] **Step 1: Creează `lib/campaigns/audit.ts`**

Fișier nou:

```ts
import { createClient } from '@supabase/supabase-js'

function admin() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false, autoRefreshToken: false } },
  )
}

export type CostType = 'total_budget' | 'agreed_fee'

/**
 * Append un rând în campaign_cost_history pentru o modificare reală de cost.
 * Idempotency: nu se inserează nimic dacă before === after.
 * Best-effort: orice eroare e logată dar nu propagată — update-ul principal
 * nu trebuie blocat de un audit failure.
 */
export async function logCostChange(params: {
  campaignId: string
  participantId?: string | null
  costType: CostType
  before: number | null
  after: number | null
  changedBy: string | null
}): Promise<void> {
  if (params.before === params.after) return

  try {
    const supabase = admin()
    const { error } = await supabase.from('campaign_cost_history').insert({
      campaign_id: params.campaignId,
      participant_id: params.participantId ?? null,
      cost_type: params.costType,
      amount_before: params.before,
      amount_after: params.after,
      changed_by: params.changedBy,
    })
    if (error) console.error('[campaign cost audit]', error.message)
  } catch (err) {
    console.error('[campaign cost audit]', err)
  }
}
```

- [ ] **Step 2: Creează `lib/scoring/audit.ts`**

Fișier nou:

```ts
import { createClient } from '@supabase/supabase-js'

export const WEIGHT_KEYS = [
  'weight_engagement_rate',
  'weight_cpv',
  'weight_audience_ro',
  'weight_punctuality',
  'weight_deliverable_quality',
  'weight_collaboration_history',
] as const

export type WeightKey = (typeof WEIGHT_KEYS)[number]
export type Weights = Record<WeightKey, number>

function admin() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false, autoRefreshToken: false } },
  )
}

/**
 * Compute diff between two weights objects. Returns only changed keys.
 */
export function diffWeights(
  before: Weights,
  after: Weights,
): Record<string, { old: number; new: number }> {
  const changes: Record<string, { old: number; new: number }> = {}
  for (const k of WEIGHT_KEYS) {
    if (before[k] !== after[k]) {
      changes[k] = { old: before[k], new: after[k] }
    }
  }
  return changes
}

/**
 * Append un rând în scoring_settings_history când există modificare reală.
 * Best-effort (eșec audit NU blochează update-ul ponderilor).
 */
export async function logWeightsChange(params: {
  before: Weights
  after: Weights
  changedBy: string | null
}): Promise<void> {
  const changes = diffWeights(params.before, params.after)
  if (Object.keys(changes).length === 0) return

  try {
    const supabase = admin()
    const { error } = await supabase.from('scoring_settings_history').insert({
      weights_before: params.before,
      weights_after: params.after,
      changes,
      changed_by: params.changedBy,
    })
    if (error) console.error('[scoring weights audit]', error.message)
  } catch (err) {
    console.error('[scoring weights audit]', err)
  }
}
```

- [ ] **Step 3: Verifică typecheck**

Run: `pnpm run typecheck`
Expected: 0 errors.

- [ ] **Step 4: Commit helpers**

```bash
git add lib/campaigns/audit.ts lib/scoring/audit.ts
git commit -m "feat(audit): helpers logCostChange + logWeightsChange best-effort"
```

---

## Task 3: Wrap PATCH endpoints + GET cost-history

**Files:**
- Modify: `app/api/campaigns/[id]/route.ts`
- Modify: `app/api/campaigns/[id]/participants/[participant_id]/route.ts`
- Modify: `app/api/admin/scoring-settings/route.ts`
- Create: `app/api/campaigns/[id]/cost-history/route.ts`

**Context:** Wrap PATCH-urile existente cu apel la helpers. PATCH-urile fetch `prev` snapshot pentru `prev.total_budget` / `prev.agreed_fee` / `prev weights`. După update success, apelează helper-ul cu before/after. GET endpoint folosește Path A scoping cu `canReadCampaign`.

- [ ] **Step 1: Wrap PATCH `/api/campaigns/[id]` cu audit pe `total_budget`**

În `app/api/campaigns/[id]/route.ts`, găsește block-ul existent care fetch `prev` (pentru `previousStatus`). Linia ~138:

```ts
  // Capture previous status to detect draft → active transition for hooks
  const { data: prev } = await supabase
    .from('campaigns')
    .select('status')
    .eq('id', id)
    .maybeSingle<{ status: string }>()
  const previousStatus = prev?.status ?? null
```

Extinde select-ul să include `total_budget`:

```ts
  // Capture previous state pentru audit + status hooks
  const { data: prev } = await supabase
    .from('campaigns')
    .select('status, total_budget')
    .eq('id', id)
    .maybeSingle<{ status: string; total_budget: number | null }>()
  const previousStatus = prev?.status ?? null
  const previousBudget = prev?.total_budget ?? null
```

În top imports adaugă:

```ts
import { logCostChange } from '@/lib/campaigns/audit'
```

După block-ul de update success (după `if (!data)` și înainte de `// Hook: campaign_started`), adaugă:

```ts
  // §5 audit cost (best-effort, nu blochează response-ul)
  if (body.total_budget !== undefined) {
    const h = await headers()
    const userId = h.get('x-user-id')
    await logCostChange({
      campaignId: id,
      costType: 'total_budget',
      before: previousBudget,
      after: data.total_budget ?? null,
      changedBy: userId,
    })
  }
```

Notă: `data.total_budget` accesibil pentru că select-ul returns toate coloanele. Verifică tipul TypeScript prin parametrul generic la `maybeSingle<>` — extinde-l să include `total_budget: number | null` dacă lipsește.

- [ ] **Step 2: Verifică typecheck după Step 1**

Run: `pnpm run typecheck`
Expected: 0 errors. Dacă apare eroare pe `data.total_budget`, extinde type-ul generic pe `maybeSingle<{ ... total_budget: number | null; ... }>` în block-ul de update (linia ~134).

- [ ] **Step 3: Wrap PATCH `/api/campaigns/[id]/participants/[participant_id]` cu audit pe `agreed_fee`**

În `app/api/campaigns/[id]/participants/[participant_id]/route.ts`, identifică PATCH handler (linia ~50+). Înainte de `update`, fetch `prev`:

Run: `grep -n "supabase.from('campaign_participants').*update\|PATCH" "app/api/campaigns/[id]/participants/[participant_id]/route.ts" | head -10`

Adaugă import la top:

```ts
import { logCostChange } from '@/lib/campaigns/audit'
```

Înainte de apelul `supabase.from('campaign_participants').update(update)`, fetch fee curent:

```ts
  const { data: prev } = await supabase
    .from('campaign_participants')
    .select('agreed_fee, campaign_id')
    .eq('id', participantId)
    .maybeSingle<{ agreed_fee: number | null; campaign_id: string }>()
  const previousFee = prev?.agreed_fee ?? null
```

(`participantId` e variabila locală din `await params` — folosește numele exact din fișier.)

După update success (verifică response are `data.agreed_fee` și `prev?.campaign_id`), adaugă:

```ts
  if (body.agreed_fee !== undefined && prev?.campaign_id) {
    const h = await headers()
    const userId = h.get('x-user-id')
    await logCostChange({
      campaignId: prev.campaign_id,
      participantId: participantId,
      costType: 'agreed_fee',
      before: previousFee,
      after: data.agreed_fee ?? null,
      changedBy: userId,
    })
  }
```

Verifică că `import { headers } from 'next/headers'` există în top; dacă nu, adaugă-l.

- [ ] **Step 4: Wrap PATCH `/api/admin/scoring-settings` cu audit weights**

În `app/api/admin/scoring-settings/route.ts`, în PATCH handler (linia 56+):

Adaugă import top:

```ts
import { logWeightsChange, type Weights, WEIGHT_KEYS } from '@/lib/scoring/audit'
```

ÎNAINTE de apelul `.update(update)` (linia 91), fetch settings curente:

```ts
  const { data: settingsBefore } = await supabase
    .from('scoring_settings')
    .select(WEIGHT_KEYS.join(', '))
    .eq('id', 1)
    .maybeSingle<Weights>()
```

DUPĂ update success (după `if (error) ... }`, în jurul liniei 103, înainte de bulk recalc), adaugă:

```ts
  // §5 audit weights (best-effort)
  if (settingsBefore && settings) {
    const after: Weights = {
      weight_engagement_rate: (settings as Record<string, number>).weight_engagement_rate,
      weight_cpv: (settings as Record<string, number>).weight_cpv,
      weight_audience_ro: (settings as Record<string, number>).weight_audience_ro,
      weight_punctuality: (settings as Record<string, number>).weight_punctuality,
      weight_deliverable_quality: (settings as Record<string, number>).weight_deliverable_quality,
      weight_collaboration_history: (settings as Record<string, number>).weight_collaboration_history,
    }
    await logWeightsChange({
      before: settingsBefore,
      after,
      changedBy: user.id,
    })
  }
```

- [ ] **Step 5: Creează `app/api/campaigns/[id]/cost-history/route.ts`**

Mkdir + create:

```bash
mkdir -p "app/api/campaigns/[id]/cost-history"
```

Fișier nou `app/api/campaigns/[id]/cost-history/route.ts`:

```ts
import { NextResponse, type NextRequest } from 'next/server'
import { createClient } from '@supabase/supabase-js'
import { getCurrentUser, canReadCampaign } from '@/lib/auth/scope'

function admin() {
  return createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false, autoRefreshToken: false } },
  )
}

export async function GET(_req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params
  const user = await getCurrentUser()
  if (!user) {
    return NextResponse.json({ ok: false, error: 'unauthorized' }, { status: 401 })
  }

  const supabase = admin()
  const { data: row } = await supabase
    .from('campaigns')
    .select('owner_id')
    .eq('id', id)
    .maybeSingle<{ owner_id: string | null }>()
  if (!row) {
    return NextResponse.json({ ok: false, error: 'not_found' }, { status: 404 })
  }
  if (!canReadCampaign(user, { owner_id: row.owner_id })) {
    return NextResponse.json({ ok: false, error: 'not_found' }, { status: 404 })
  }

  const { data: entries, error } = await supabase
    .from('campaign_cost_history')
    .select(`
      id, cost_type, amount_before, amount_after, changed_at,
      changed_by:team_members!campaign_cost_history_changed_by_fkey(id, name),
      participant:campaign_participants(id, account_handle, platform, influencer:influencers(name))
    `)
    .eq('campaign_id', id)
    .order('changed_at', { ascending: false })
    .limit(50)

  if (error) {
    return NextResponse.json(
      { ok: false, error: 'server_error', detail: error.message },
      { status: 500 },
    )
  }
  return NextResponse.json({ ok: true, entries: entries ?? [] })
}
```

- [ ] **Step 6: Verifică typecheck + lint**

Run: `pnpm run typecheck`
Expected: 0 errors. Dacă apare eroare pe FK name `campaign_cost_history_changed_by_fkey`, run:
```sql
-- via Supabase MCP execute_sql
SELECT conname FROM pg_constraint WHERE conrelid = 'campaign_cost_history'::regclass AND contype = 'f';
```
Foloseşte numele exact returnat.

Run: `pnpm run lint`
Expected: 0 errors.

- [ ] **Step 7: Commit backend changes**

```bash
git add app/api/campaigns/[id]/route.ts app/api/campaigns/[id]/participants/[participant_id]/route.ts app/api/admin/scoring-settings/route.ts app/api/campaigns/[id]/cost-history/route.ts
git commit -m "feat(api): wrap PATCH cu audit cost + ponderi + GET cost-history"
```

---

## Task 4: UI Tab "Istoric" pe campanie

**Files:**
- Create: `app/campaigns/[id]/campaign-audit-tab.tsx`
- Modify: `app/campaigns/[id]/tabs-shell.tsx`
- Modify: `app/campaigns/[id]/page.tsx`

**Context:** Tabs-shell e client component cu 5 tab-uri. Adăugăm a 6-a "Istoric" cu component nou care fetch on-mount.

- [ ] **Step 1: Creează `app/campaigns/[id]/campaign-audit-tab.tsx`**

Fișier nou:

```tsx
'use client'

import { useEffect, useState } from 'react'
import { formatEur } from '@/lib/influencers/format'

type Entry = {
  id: string
  cost_type: 'total_budget' | 'agreed_fee'
  amount_before: number | null
  amount_after: number | null
  changed_at: string
  changed_by: { id: string; name: string } | null
  participant: {
    id: string
    account_handle: string | null
    platform: string
    influencer: { name: string } | null
  } | null
}

type State =
  | { kind: 'loading' }
  | { kind: 'ok'; entries: Entry[] }
  | { kind: 'error'; message: string }

function formatRelative(iso: string): string {
  const date = new Date(iso)
  const now = Date.now()
  const diffMs = now - date.getTime()
  const diffMin = Math.round(diffMs / 60000)
  if (diffMin < 1) return 'acum câteva secunde'
  if (diffMin < 60) return `acum ${diffMin} min`
  const diffH = Math.round(diffMin / 60)
  if (diffH < 24) return `acum ${diffH}h`
  const diffD = Math.round(diffH / 24)
  if (diffD < 7) return `acum ${diffD} zile`
  return date.toLocaleDateString('ro-RO', { day: 'numeric', month: 'short', year: 'numeric' })
}

function formatAmount(n: number | null): string {
  if (n == null) return '—'
  return formatEur(n)
}

export function CampaignAuditTab({ campaignId }: { campaignId: string }) {
  const [state, setState] = useState<State>({ kind: 'loading' })

  useEffect(() => {
    let cancelled = false
    fetch(`/api/campaigns/${campaignId}/cost-history`)
      .then(async (res) => {
        const data = await res.json().catch(() => ({}))
        if (cancelled) return
        if (res.ok && data.entries) {
          setState({ kind: 'ok', entries: data.entries as Entry[] })
        } else {
          setState({ kind: 'error', message: data.error ?? 'server_error' })
        }
      })
      .catch((err) => {
        if (!cancelled) {
          setState({ kind: 'error', message: err instanceof Error ? err.message : 'unknown' })
        }
      })
    return () => {
      cancelled = true
    }
  }, [campaignId])

  if (state.kind === 'loading') {
    return <div className="text-sm text-stone-500">Se încarcă istoricul...</div>
  }
  if (state.kind === 'error') {
    return <div className="text-sm text-rose-600">Eroare: {state.message}</div>
  }
  if (state.entries.length === 0) {
    return (
      <div className="text-sm text-stone-500 bg-white border border-stone-200 rounded-xl p-6 text-center">
        Niciun istoric pentru această campanie.
      </div>
    )
  }

  return (
    <div className="bg-white border border-stone-200 rounded-xl overflow-hidden">
      <table className="w-full text-sm">
        <thead className="bg-stone-50 border-b border-stone-200">
          <tr className="text-left text-[11px] font-semibold uppercase tracking-[0.06em] text-stone-500">
            <th className="px-4 py-3">Tip</th>
            <th className="px-4 py-3">Detaliu</th>
            <th className="px-4 py-3 text-right">De la</th>
            <th className="px-4 py-3 text-right">La</th>
            <th className="px-4 py-3">Modificat de</th>
            <th className="px-4 py-3">Când</th>
          </tr>
        </thead>
        <tbody className="divide-y divide-stone-100">
          {state.entries.map((e) => (
            <tr key={e.id} className="hover:bg-stone-50 transition-colors">
              <td className="px-4 py-3">
                <span className="text-[10px] uppercase tracking-wide font-medium px-2 py-0.5 rounded-full bg-amber-100 text-amber-900">
                  {e.cost_type === 'total_budget' ? 'Buget' : 'Fee'}
                </span>
              </td>
              <td className="px-4 py-3 text-stone-600">
                {e.cost_type === 'agreed_fee' && e.participant
                  ? `${e.participant.influencer?.name ?? 'Ad-hoc'} (${e.participant.platform})`
                  : '—'}
              </td>
              <td className="px-4 py-3 text-stone-600 text-right tabular-nums">{formatAmount(e.amount_before)}</td>
              <td className="px-4 py-3 text-stone-900 text-right tabular-nums font-medium">{formatAmount(e.amount_after)}</td>
              <td className="px-4 py-3 text-stone-600">{e.changed_by?.name ?? '—'}</td>
              <td className="px-4 py-3 text-stone-500" title={new Date(e.changed_at).toLocaleString('ro-RO')}>
                {formatRelative(e.changed_at)}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  )
}
```

- [ ] **Step 2: Extend `app/campaigns/[id]/tabs-shell.tsx`**

Înlocuiește conținutul complet:

```tsx
'use client'

import { type ReactNode } from 'react'
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/lib/ui'

export function CampaignTabsShell({
  details,
  participants,
  deliverables,
  milestones,
  tasks,
  audit,
}: {
  details: ReactNode
  participants: ReactNode
  deliverables: ReactNode
  milestones: ReactNode
  tasks: ReactNode
  audit: ReactNode
}) {
  return (
    <Tabs defaultValue="details" className="space-y-6">
      <div className="sticky top-14 z-10 -mx-4 sm:mx-0 bg-stone-50/85 backdrop-blur-md">
        <TabsList>
          <TabsTrigger value="details">Detalii</TabsTrigger>
          <TabsTrigger value="participants">Participanți</TabsTrigger>
          <TabsTrigger value="deliverables">Livrabile</TabsTrigger>
          <TabsTrigger value="milestones">Etape</TabsTrigger>
          <TabsTrigger value="tasks">Tasks</TabsTrigger>
          <TabsTrigger value="audit">Istoric</TabsTrigger>
        </TabsList>
      </div>
      <TabsContent value="details">{details}</TabsContent>
      <TabsContent value="participants">{participants}</TabsContent>
      <TabsContent value="deliverables">{deliverables}</TabsContent>
      <TabsContent value="milestones">{milestones}</TabsContent>
      <TabsContent value="tasks">{tasks}</TabsContent>
      <TabsContent value="audit">{audit}</TabsContent>
    </Tabs>
  )
}
```

- [ ] **Step 3: Pasează `audit` slot din `app/campaigns/[id]/page.tsx`**

Localizează unde `<CampaignTabsShell>` e renderat (probabil în JSX-ul page-ului). Adaugă prop `audit`:

Run: `grep -n "CampaignTabsShell\|details=" "app/campaigns/[id]/page.tsx" | head -5`

Adaugă import:
```tsx
import { CampaignAuditTab } from './campaign-audit-tab'
```

În locul unde `<CampaignTabsShell ... tasks={...}` se închide, adaugă prop nou:

```tsx
<CampaignTabsShell
  details={...}
  participants={...}
  deliverables={...}
  milestones={...}
  tasks={...}
  audit={<CampaignAuditTab campaignId={campaign.id} />}
/>
```

- [ ] **Step 4: Verifică typecheck**

Run: `pnpm run typecheck`
Expected: 0 errors.

- [ ] **Step 5: Verifică lint**

Run: `pnpm run lint`
Expected: 0 errors.

- [ ] **Step 6: Commit UI campanie**

```bash
git add app/campaigns/[id]/campaign-audit-tab.tsx app/campaigns/[id]/tabs-shell.tsx app/campaigns/[id]/page.tsx
git commit -m "feat(campaigns): tab Istoric pe /campaigns/[id] cu cost changes"
```

---

## Task 5: UI Section istoric ponderi pe admin

**Files:**
- Create: `app/admin/scoring-settings/weights-history-section.tsx`
- Modify: `app/admin/scoring-settings/page.tsx`

**Context:** Server-side fetch în page.tsx (deja server component) + section component nou care primește entries ca prop.

- [ ] **Step 1: Creează `app/admin/scoring-settings/weights-history-section.tsx`**

Fișier nou:

```tsx
import { CRITERIA_LABELS } from '@/lib/scoring/types'

type Entry = {
  id: string
  changes: Record<string, { old: number; new: number }> | null
  changed_at: string
  changed_by: { id: string; name: string } | null
}

function formatRelative(iso: string): string {
  const date = new Date(iso)
  const now = Date.now()
  const diffMin = Math.round((now - date.getTime()) / 60000)
  if (diffMin < 1) return 'acum câteva secunde'
  if (diffMin < 60) return `acum ${diffMin} min`
  const diffH = Math.round(diffMin / 60)
  if (diffH < 24) return `acum ${diffH}h`
  const diffD = Math.round(diffH / 24)
  if (diffD < 7) return `acum ${diffD} zile`
  return date.toLocaleDateString('ro-RO', { day: 'numeric', month: 'short', year: 'numeric' })
}

function formatChanges(changes: Record<string, { old: number; new: number }> | null): string {
  if (!changes) return '—'
  const parts: string[] = []
  for (const [key, val] of Object.entries(changes)) {
    // key e weight_xxx; căutăm label-ul din CRITERIA_LABELS (mapează după criteria key)
    const criteriaKey = key.replace(/^weight_/, '') // ex: weight_engagement_rate → engagement_rate
    const label =
      (CRITERIA_LABELS as Record<string, string>)[criteriaKey] ??
      criteriaKey.replace(/_/g, ' ')
    parts.push(`${label}: ${val.old} → ${val.new}`)
  }
  return parts.join(', ')
}

export function WeightsHistorySection({ entries }: { entries: Entry[] }) {
  return (
    <section className="bg-white border border-stone-200 rounded-xl p-6 mt-6">
      <h2 className="font-display text-lg text-stone-900 mb-4">Istoric modificări ponderi</h2>
      {entries.length === 0 ? (
        <p className="text-sm text-stone-500">Ponderile nu au fost modificate.</p>
      ) : (
        <table className="w-full text-sm">
          <thead className="border-b border-stone-200">
            <tr className="text-left text-[11px] font-semibold uppercase tracking-[0.06em] text-stone-500">
              <th className="py-2">Criterii schimbate</th>
              <th className="py-2">Modificat de</th>
              <th className="py-2">Când</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-stone-100">
            {entries.map((e) => (
              <tr key={e.id}>
                <td className="py-2 text-stone-700">{formatChanges(e.changes)}</td>
                <td className="py-2 text-stone-600">{e.changed_by?.name ?? '—'}</td>
                <td className="py-2 text-stone-500" title={new Date(e.changed_at).toLocaleString('ro-RO')}>
                  {formatRelative(e.changed_at)}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      )}
    </section>
  )
}
```

Notă: dacă `CRITERIA_LABELS` nu există în `lib/scoring/types.ts` cu shape `{ engagement_rate: 'Engagement', cpv: 'CPV', ... }`, fă fallback la replace_underscore_with_space (vezi codul). Run `grep "CRITERIA_LABELS" lib/scoring/types.ts` pentru a verifica.

- [ ] **Step 2: Fetch history în `app/admin/scoring-settings/page.tsx`**

Run: `grep -n "scoring_settings\|ScoringSettingsUI" "app/admin/scoring-settings/page.tsx" | head -10`

În page.tsx (server component), găsește fetch-ul actual al `scoring_settings`. Adaugă o a doua interogare:

```ts
const [{ data: settings }, { data: history }] = await Promise.all([
  supabase.from('scoring_settings').select('*').eq('id', 1).maybeSingle(),
  supabase
    .from('scoring_settings_history')
    .select('id, changes, changed_at, changed_by:team_members(id, name)')
    .order('changed_at', { ascending: false })
    .limit(10),
])
```

Adaugă import:

```tsx
import { WeightsHistorySection } from './weights-history-section'
```

În JSX-ul page-ului, sub `<ScoringSettingsUI ...>`, adaugă:

```tsx
<WeightsHistorySection entries={(history ?? []) as never} />
```

(Cast la `never` doar dacă type complex; altfel folosește tipul exact `Entry[]` din component.)

- [ ] **Step 3: Verifică typecheck**

Run: `pnpm run typecheck`
Expected: 0 errors. Dacă FK join pe `team_members` returns array, mapă la `changed_by: row.changed_by?.[0] ?? null` (similar pattern din PDF route Task 5 anterior).

- [ ] **Step 4: Verifică lint**

Run: `pnpm run lint`
Expected: 0 errors.

- [ ] **Step 5: Commit UI admin**

```bash
git add app/admin/scoring-settings/weights-history-section.tsx app/admin/scoring-settings/page.tsx
git commit -m "feat(admin): section istoric modificări ponderi pe /admin/scoring-settings"
```

---

## Task 6: Verificare finală + push

**Files:**
- N/A (verification + git)

- [ ] **Step 1: Build production**

Run: `pnpm run build`
Expected: success.

- [ ] **Step 2: Smoke SQL — verifică tabele goale**

Via Supabase MCP `execute_sql`:
```sql
SELECT COUNT(*) FROM campaign_cost_history;
SELECT COUNT(*) FROM scoring_settings_history;
```

Expected: 0 pentru ambele înainte de smoke.

- [ ] **Step 3: Smoke API local — modific buget campanie**

`pnpm dev`. În browser dev tools console (după login):

```js
// înlocuieşte cu un id real de campanie
fetch('/api/campaigns/<id>', {
  method: 'PATCH',
  headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ total_budget: 1500 }),
}).then(r => r.json()).then(console.log)
```

Expected: `{ ok: true, campaign: {...} }`.

Apoi verifică history:
```js
fetch('/api/campaigns/<id>/cost-history')
  .then(r => r.json()).then(console.log)
```

Expected: entries cu un row nou cost_type='total_budget' + amount_after=1500.

- [ ] **Step 4: Smoke API local — modific ponderi**

```js
fetch('/api/admin/scoring-settings', {
  method: 'PATCH',
  headers: { 'content-type': 'application/json' },
  body: JSON.stringify({ weight_engagement_rate: 30 }),
}).then(r => r.json()).then(console.log)
```

Expected: response OK + bulk recalc count.

Verifică în SQL via MCP:
```sql
SELECT changes FROM scoring_settings_history ORDER BY changed_at DESC LIMIT 1;
```

Expected: `{"weight_engagement_rate": {"old": 25, "new": 30}}` (sau valoarea existentă în prod).

- [ ] **Step 5: Push pe main**

```bash
git push origin main
```

- [ ] **Step 6: Verifică GHA deploy**

Run: `gh run list --limit 1 --workflow=deploy.yml --json databaseId,status`

Folosește `gh run watch <id> --exit-status` ca să aştepţi finalizarea.

- [ ] **Step 7: Smoke live pe iPhone PWA**

- `/campaigns/[id]` → click tab "Istoric" → vezi cost changes (sau empty state)
- Modific buget pe detail page → revin la tab Istoric → vezi entry nou
- `/admin/scoring-settings` (cu owner login) → scroll jos → vezi section "Istoric modificări ponderi"

---

## Self-Review

**Spec coverage:**
- §2.1 Cost history per campanie → Task 1 (DB) + Task 3 Steps 1-3 (audit insert) + Task 3 Step 5 (GET) + Task 4 (UI tab)
- §2.2 Weights history → Task 1 (DB) + Task 3 Step 4 (audit insert) + Task 5 (UI section)
- §2.3 UI tab Istoric → Task 4
- §2.4 UI section ponderi → Task 5
- §2.5 GET endpoint → Task 3 Step 5
- §3 + §4 Migrations → Task 1
- §5 Backend → Task 2 (helpers) + Task 3 (wrap)
- §6 UI → Task 4 + Task 5
- §9 DoD → Task 6

**Placeholder scan:** Niciun "TBD" / "implement later". Task 3 Step 2 zice "extinde type-ul generic dacă lipsește" — acceptabil pentru ajustare type strict locală. Task 3 Step 3 cere `grep` pentru numele exact `participantId` — verificare structură reală (nu inventez).

**Type consistency:**
- `CostType` definit în Task 2, folosit în Task 3 ✅
- `Weights` + `WEIGHT_KEYS` definite în Task 2, folosite în Task 3 Step 4 ✅
- `logCostChange` + `logWeightsChange` signatures consistente cross-task ✅
- `Entry` shape în Task 4 match `GET /api/campaigns/[id]/cost-history` response din Task 3 Step 5 ✅
- `formatRelative` duplicat în Task 4 + Task 5 — acceptabil pentru first-pass (DRY-extract dacă apare 3rd usage)

**Migration ordering:** Task 1 (DB) PRECEDĂ Task 2 (helpers folosesc tabelele). Task 3 (wrap PATCH) PRECEDĂ Task 4+5 (UI fetch din tabele). Plan respectă ordinea.

**Risk reminder:** spec §8 — audit failure NU blochează update (try/catch în helpers), idempotency (insert doar la diff real), CHECK constraint enforce consistency.
